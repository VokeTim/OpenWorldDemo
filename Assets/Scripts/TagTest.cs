using UnityEngine;

[MyTagTest(12,"testTage")]
public class TagTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
